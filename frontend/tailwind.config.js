// Used to configure and customize Tailwind CSS settings
// E.g. Specifying the files to scan for class names, Extending the default theme, and Adding plugins

module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
};
